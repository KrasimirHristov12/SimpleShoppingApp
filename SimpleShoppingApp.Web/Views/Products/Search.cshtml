@model IEnumerable<ListProductsViewModel>
@{
    ViewData["Title"] = "Search";
}

<h2>Search term: @ViewData["Name"] (@Model.Count() found)</h2>

@if (Model.Count() == 0)
{
    <h5>No products found</h5>
}
else
{
    <div class="row">
        <partial name="_ListProducts" />
    </div>
   
}
